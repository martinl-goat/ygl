# YGL – You've goat logs!

This is YGL, a simple NDJSON log viewer.

## Notes

- Started with `react-ts` vite template, cleaned it up a bit, added prettier.
- Added first attempt at a streaming downloader plus basic table.
